export enum GameStatusesSortingEnum {
  GAME_NAME = 1,
  GAME_ID,
  EXTERNAL_ID,
  PROVIDER_NAME,
  PROVIDER_ID,
  SUBTYPE,
  TYPE,
  VOLATILITY,
  RTP,
  CLASS,
  CREATION_DATE,
  CREATED_BY,
  RELEASE_DATE,
  CATEGORY
}
